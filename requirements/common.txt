# Core Django
Django~=5.1.6
djangorestframework~=3.15.2
asgiref~=3.8.1
channels~=4.2.0
channels-redis~=4.2.0
python-socketio~=5.11.0
uvicorn[standard]~=0.34.0

# Authentication
dj-rest-auth~=7.0.1
PyJWT<2.10.0
django-allauth~=65.4.1

# Database (Optional, if using PostgreSQL)
psycopg2-binary~=2.9.10

# Environment Variables (Optional)
django-environ~=0.12.0

# CORS Headers (Optional, for API development)
django-cors-headers~=3.13.0

# error monitoring
sentry-sdk~= 2.26.1

# jinja2 is required in prod, because sentry requires it
Jinja2~=3.1.6

# DRF
djangorestframework-camel-case~=1.4.2
drf-spectacular~=0.28.0
djangorestframework_simplejwt~=5.5.0

requests~=2.32.3
httpx~=0.28.1

# Document parsing
# Docling produces the structured document model (elements in reading order,
# tables as markdown, picture positions and captions). Importing it loads a
# layout model on top of the torch stack sentence-transformers already brings
# in, so it is imported lazily and only ever reached in the RQ worker that
# parses uploads — see core/services/document_parsers/__init__.py.
docling~=2.117.0
# PyPDF2 remains the flat-text fallback when a Docling conversion fails.
PyPDF2~=3.0.1
# Renders a page region back to an image, so a picture the parser located can
# be cropped out of the original on demand instead of being stored twice.
pypdfium2~=5.12.1
weasyprint~=62.3
pydyf<0.12.0
markdown~=3.7
python-pptx~=1.0.2

# Pinecone
pinecone~=6.0.1

# OpenAI (also used by the memory writer + text-embedding-3-small)
openai>=2.8.0
tiktoken~=0.8.0

# Claude/Anthropic (0.75.0+ includes structured outputs beta)
anthropic~=0.75.0

# Google Gemini (new unified SDK)
google-genai~=1.75.0

# LLaMA (via Ollama)
# ollama~=0.4.2 - upgrading it below to avoid conflict with httpx
ollama>=0.6.1

rq==2.2.0
django-rq==3.0.0
rq-scheduler==0.14.0
redis==5.2.1

pycryptodome==3.22.0


# weaviate
weaviate-client==4.14.1

# Local cross-encoder reranker (Track A). Imported lazily when advanced retrieval
# runs; pulls in torch transitively (~CPU/MPS build).
sentence-transformers~=5.6.0

#email
resend~=2.7.0

# Text splitting (document chunking in core/services/document_processor.py)
langchain-text-splitters>=0.4.0

# Spreadsheet processing
openpyxl~=3.1.5
# Optional legacy .xls support (used only if available)
xlrd~=2.0.1

# Video processing (for frame extraction and audio transcription)
opencv-python~=4.10.0
Pillow~=11.1.0
ffmpeg-python~=0.2.0

# Cross-conversation memory layer: pgvector column type on MemoryRecord
pgvector~=0.3.6
# Postgres driver (psycopg3)
psycopg[binary]~=3.2.0

# Energy/environmental impact tracking for LLM calls
ecologits>=0.10.0

# SyftBox (distributed file storage)
syft-core
syft-perm
