diff --git a/agent/tool_executor.py b/agent/tool_executor.py
index a5729a2e8..8817a5bc3 100644
--- a/agent/tool_executor.py
+++ b/agent/tool_executor.py
@@ -942,6 +942,7 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
                         "tool.completed", function_name, None, None,
                         duration=tool_duration, is_error=is_error,
                         result=function_result,
+                        tool_call_id=getattr(tc, "id", "") or "",
                     )
                 except Exception as cb_err:
                     logging.debug(f"Tool progress callback error: {cb_err}")
@@ -1651,6 +1652,7 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
                     "tool.completed", function_name, None, None,
                     duration=tool_duration, is_error=_is_error_result,
                     result=function_result,
+                    tool_call_id=getattr(tool_call, "id", "") or "",
                 )
             except Exception as cb_err:
                 logging.debug(f"Tool progress callback error: {cb_err}")
diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
index a1b066a85..c0f0701d4 100644
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -5967,6 +5967,7 @@ class APIServerAdapter(BasePlatformAdapter):
                     "tool": tool_name,
                     "duration": round(kwargs.get("duration", 0), 3),
                     "error": kwargs.get("is_error", False),
+                    "toolCallId": kwargs.get("tool_call_id", ""),
                 })
             elif event_type == "reasoning.available":
                 _push({
diff --git a/tools/mcp_tool.py b/tools/mcp_tool.py
index 28dd5e917..631dbce27 100644
--- a/tools/mcp_tool.py
+++ b/tools/mcp_tool.py
@@ -4619,8 +4619,17 @@ def _make_tool_handler(server_name: str, tool_name: str, tool_timeout: float):
                 # task, which doesn't inherit our contextvars) can replay
                 # it and detect the gateway platform / session for routing.
                 server._pending_call_context = contextvars.copy_context()
+                # Forward this call's unique tool_use id via MCP `_meta` so a
+                # DARE-owned server can attribute the call to the exact tool_use
+                # it belongs to (the same id carried on the tool.completed event).
+                from tools.approval import _approval_tool_call_id
+
+                _call_id = _approval_tool_call_id.get("")
+                _meta = {"dareCall": _call_id} if _call_id else None
                 try:
-                    result = await server.session.call_tool(tool_name, arguments=args)
+                    result = await server.session.call_tool(
+                        tool_name, arguments=args, meta=_meta
+                    )
                 finally:
                     server._pending_call_context = None
             # The RPC round-trip completed — the session is demonstrably
